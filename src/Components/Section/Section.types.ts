export type Props = {
  /**
   * id for the root element.
   */

  id: string;
  /**
   * Title of the section.
   */
  title?: string;

  /**
   * Sub title for the section.
   */
  subTitle?: string;
};
export type Props = {};

export type State = {
  /**
   * USD cost of one Bitcoin before the change.
   */
  startPrice: number;

  /**
   * USD cost of one Bitcoin after the change.
   */
  endPrice: number;
};